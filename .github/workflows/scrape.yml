      - name: Scrape BB
        run: node scrape/prca.js BB 2025 > data/BB.json || (echo "BB failed, continuing"; echo "[]" > data/BB.json)
        timeout-minutes: 6
        continue-on-error: true

      - name: Scrape SB
        run: node scrape/prca.js SB 2025 > data/SB.json || (echo "SB failed, continuing"; echo "[]" > data/SB.json)
        timeout-minutes: 6
        continue-on-error: true

      - name: Scrape BR
        run: node scrape/prca.js BR 2025 > data/BR.json || (echo "BR failed, continuing"; echo "[]" > data/BR.json)
        timeout-minutes: 6
        continue-on-error: true

      - name: Scrape TD
        run: node scrape/prca.js TD 2025 > data/TD.json || (echo "TD failed, continuing"; echo "[]" > data/TD.json)
        timeout-minutes: 6
        continue-on-error: true

      - name: Scrape SW
        run: node scrape/prca.js SW 2025 > data/SW.json || (echo "SW failed, continuing"; echo "[]" > data/SW.json)
        timeout-minutes: 6
        continue-on-error: true

      - name: Scrape TRH
        run: node scrape/prca.js TRH 2025 > data/TRH.json || (echo "TRH failed, continuing"; echo "[]" > data/TRH.json)
        timeout-minutes: 6
        continue-on-error: true

      - name: Scrape TRL
        run: node scrape/prca.js TRL 2025 > data/TRL.json || (echo "TRL failed, continuing"; echo "[]" > data/TRL.json)
        timeout-minutes: 6
        continue-on-error: true

      - name: Scrape LBR
        run: node scrape/prca.js LBR 2025 > data/LBR.json || (echo "LBR failed, continuing"; echo "[]" > data/LBR.json)
        timeout-minutes: 6
        continue-on-error: true

      - name: Build ALL (merge)
        run: node scrape/prca.js ALL 2025 > data/all.json || (echo "[]" > data/all.json)
        timeout-minutes: 10
        continue-on-error: true

      - name: Upload debug artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: debug-pages
          path: |
            data/debug/**
            data/*.json

      - name: Commit JSON
        run: |
          git config user.name "github-actions"
          git config user.email "actions@users.noreply.github.com"
          git add data/*.json
          git commit -m "Daily standings update" || echo "No changes"
          git push
